import 'package:flutter/material.dart';

class AnggotaNavigationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Anggota Navigation Screen", style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
