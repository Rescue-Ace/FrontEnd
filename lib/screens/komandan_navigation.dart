import 'package:flutter/material.dart';

class KomandanNavigationScreen extends StatelessWidget {
  const KomandanNavigationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Komandan Navigation Screen", style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
