import 'package:flutter/material.dart';

class DamkarNavigationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Damkar Navigation Screen", style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
